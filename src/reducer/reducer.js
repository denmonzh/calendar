const initialState = {
  modal: false,
  pending: null,
  items: [],
};


const reducerItems = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducerItems;
