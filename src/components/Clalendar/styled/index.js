import styled from 'styled-components';
import {
  Flex,
} from 'web-styled';

export const CalendarContainer = styled(Flex).attrs({
  justify: 'center',
  align: 'center',
  column: 'column',
})`
    width: 100%;
`;
